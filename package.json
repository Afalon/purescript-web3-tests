{
  "name": "purescript-web3-tests",
  "version": "1.0.0",
  "description": "",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generator": "cd generator && npm i && pulp run -- --abis ../build/contracts --dest ../src --truffle",
    "bower": "./node_modules/.bin/bower --quiet cache clean && ./node_modules/.bin/bower --quiet install",
    "postinstall": "npm run generator && npm run bower"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bignumber.js": "git+https://github.com/frozeman/bignumber.js-nolookahead.git",
    "crypto-js": "^3.1.8",
    "pulp": "^11.0.2"
  }
}
